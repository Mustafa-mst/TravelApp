export type Locatable = {
  id: string;
  name: string;
  latitude: number | null;
  longitude: number | null;
};

export type Coordinates = {
  latitude: number | null;
  longitude: number | null;
};
